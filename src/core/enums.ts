export enum WindowType {
  NORMAL,
  FULLSCREEN,
  BORDER_LESS,
}

export enum WindowLevel {
  TOP,
  MIDDLE,
  BOTTOM,
}

export enum WindowStatus {
  NORMAL,
  MINIMIZED,
  MAXIMIZED,
}

export enum CropFlag {
  TITLE_BAR = 0,
  TOP = 1,
  RIGHT = 2,
  BOTTOM = 4,
  LEFT = 8,
}